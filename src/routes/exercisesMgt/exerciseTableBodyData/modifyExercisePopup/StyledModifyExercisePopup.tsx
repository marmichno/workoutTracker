import { Grid, Paper } from '@mui/material';
import styled from 'styled-components';

export const StyledModifyExercisePopupContainer = styled(Grid)`
  && {
    width: 100vw;
    height: 100vh;
    position: absolute;
    z-index: 999;
    display: flex;
    align-items: center;
    justify-content: center;
    left: 0;
    top: 0;
  }
`;

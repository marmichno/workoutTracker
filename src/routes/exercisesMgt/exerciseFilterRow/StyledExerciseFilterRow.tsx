import styled from 'styled-components';
import { Grid } from '@mui/material';

export const StyledExerciseFilterRowContainer = styled(Grid)`
  && {
    height: 10%;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }
`;

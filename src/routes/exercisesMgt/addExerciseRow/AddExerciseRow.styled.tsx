import { Grid } from '@mui/material';
import styled from 'styled-components';

export const AddNewExerciseRowContainer = styled(Grid)`
  && {
    width: 100%;
    height: 10%;
    display: flex;
    align-items: center;
    justify-content: center;

    h6 {
      margin-right: 1em;
    }
  }
`;
